name: 🧪 Test Workflow

on:
  push:
    branches:
      - '**'                # Triggert auf allen Branches
  workflow_dispatch:         # Erlaubt manuellen Start

jobs:
  test-job:
    name: 📦 Einfacher Testjob
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ GitHub Actions läuft
        run: echo "✅ Workflow läuft! Ausgelöst durch ${{ github.event_name }} auf Branch ${{ github.ref }}"
